language: go
sudo: false

go:
  - "1.11"

before_install:
  - go version
  - export GO111MODULE=on
  - go get golang.org/x/tools/cmd/goimports

install:
  - go build ./cmd/...

script:
  - go vet ./cmd/...
  - diff <(goimports -d .) <(printf "")

